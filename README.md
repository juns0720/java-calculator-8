# java-calculator-precourse

## 요구사항
입력한 문자열에서 숫자를 추출하여 더하는 계산기를 구현한다.
 - 쉼표(,) 또는 콜론(:)을 구분자로 가지는 문자열이라면 구분자를 기준으로 분리한 숫자의 합을 반환한다.
 - 기본 구분자(쉼표, 콜론) 외에 커스텀 구분자를 지정할 수 있다. 커스텀 구분자는 문자열 앞부분의 "//"와 "\n" 사이에 위치하는 문자를 커스텀 구분자로 사용한다.
   - 예를 들어 "//;\n1;2;3"과 같이 값을 입력한 경우, 커스텀 구분자는 세미콜론(;)이며, 결과 값은 6이 반환되어야 한다.
 - 사용자가 잘못된 값을 입력할 경우 IllegalArgumentException을 발생시킨 후 어플리케이션을 종료한다.

## 기능 목록
### 문자열 입력
 - `덧셈할 문자열을 입력해 주세요.` 메세지와 함께 문자열이 입력된다.
   - 잘못된 입력이 주어질 경우 `IllegalArgumentException` 예외를 발생한다.
     
### 커스텀 구분자 추가
 - 문자열을 검사하는 중 커스텀 구분자를 발견하면 새로운 구분자로 추가한다.

### 문자열 파싱
 - 현재까지 사용 가능한 구분자를 통해 문자열을 파싱하여 숫자를 도출한다.
 - //;\n 형식의 커스텀 구분자 문자열이 들어오지 않았을 경우 `IllegalArgumentException` 예외를 발생한다.
   
### 결과 출력
 - 도출된 숫자들을 통해 합을 계산한다.
 - 도출된 숫자가 음수인 경우 `IllegalArgumentException` 예외를 발생한다.
 - 구분자를 통해 제대로 숫자가 도출되지 않았을 경우 `IllegalArgumentException` 예외를 발생한다.
 - `결과 :` 메세지와 함께 결과가 출력된다.
 - 도출된 숫자가 존재하지 않을 경우 0을 출력한다.

## 예외 처리
 - 현재 존재하지 않는 구분자가 발견될 경우
 - 커스텀 구분자 입력이 제대로 들어오지 않았을 경우
 - 음수인 숫자가 입력으로 들어왔을 경우
